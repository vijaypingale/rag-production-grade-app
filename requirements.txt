# =========================================================
# Core LangChain stack
# =========================================================
# Pinned to the stable 0.3.x line (NOT 1.x). This is the version
# RAGAS 0.2.x is built and tested against — a common enterprise
# choice for teams that standardized on LangChain ~1-2 years ago.
# Do NOT bump to langchain 1.x without re-validating RAGAS compat.
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.31
langchain-text-splitters==0.3.11
langchain-experimental==0.3.4  # SemanticChunker lives here

# =========================================================
# Embedding providers
# =========================================================
langchain-aws==0.2.35          # AWS Bedrock embeddings (primary)
langchain-openai==0.2.14       # OpenAI embeddings + ChatOpenAI
boto3                          # transitive but pinned for clarity

# =========================================================
# Evaluation (Section 10)
# =========================================================
ragas==0.2.15                  # offline RAG evaluation (faithfulness, etc.)
datasets                       # RAGAS EvaluationDataset backing

# =========================================================
# Document loaders
# =========================================================
pymupdf                        # PDF (PyMuPDFLoader)
docx2txt                       # DOCX  (Docx2txtLoader)
# unstructured[md,html]         # uncomment to enable HTML / Markdown loaders
# beautifulsoup4                # required by BSHTMLLoader

# =========================================================
# Vector store
# =========================================================
faiss-cpu                      # in-process FAISS (swap for faiss-gpu on GPU hosts)
# opensearch-py                  # uncomment to enable OpenSearch vector store

# =========================================================
# Web framework
# =========================================================
fastapi
uvicorn
python-multipart               # required for FastAPI UploadFile
pydantic                       # request schemas

# =========================================================
# Observability / utilities
# =========================================================
structlog
tiktoken
python-dotenv
numpy

rank-bm25                      # BM25 sparse retriever (for hybrid retrieval)
cohere                         # Cohere Rerank v3 reranker client

# =========================================================
# Observability (Section 12)
# =========================================================
opentelemetry-api              # vendor-neutral tracing API
opentelemetry-sdk              # tracer provider + console exporter
# opentelemetry-exporter-otlp  # uncomment for Step 2 (Datadog/Grafana OTLP export)

# =========================================================
# Testing
# =========================================================
pytest                         # unit tests (deterministic components)